# Busyworld — Hand-off Guide

A complete orientation for anyone (human or a fresh AI session) picking up this
project. Read this first, then `docs/ARCHITECTURE.md`.

## 1. What this is
A living 2D pixel-art town (Godot 4.6) whose citizens are driven by local LLMs
(Ollama) via a Python "brain" server. Citizens perceive an embodied world, act,
talk, remember (SQLite, so they persist forever), have needs (hunger/coins), form
relationships, go indoors, and take on the founder's requests for a coin reward.
The long-term goal: grow to many towns/cities, give citizens real eyesight, and
let them run a real money-making venture from a building in town.

## 0. Git workflow (important)
The founder wants **`main` kept current**: once a change looks good (verified with
a screenshot / runs clean), **commit and push straight to `main`**. Work happens on
`claude/confident-fermat-qvx5g1`, which is kept in sync with `main` (fast-forward).
Engine: **Godot 4.6**. End commit messages with the project's Co-Authored-By /
Claude-Session trailers (see existing commits).

## 2. Current state (what's done)
- **Town**: generated by `tools/build_town.py` → bakes `godot/assets/ground/
  town_ground.png` + writes `godot/data/town_layout.json`. A 4-citizen town: a
  plaza/fountain, a grand **Town Center**, a **Print Shop** (the venture), and
  **four homes**. Coherent house/civic/shop exteriors, day/night, parked side-view
  cars, sidewalk spurs to every door.
- **Citizens**: 4 in `godot/data/citizens.json` (shared by Godot + brain) — Mara
  (Coordinator), Theo (Designer), Iris (Maker), Sam (Seller); 2 women, 2 men.
  Animated from LimeZu premade sheets, A* pathfinding, nameplates, speech bubbles,
  ground shadows, configurable count (`--agents N`).
- **Interiors with correct collision**: `tools/build_interiors.py` derives each
  room's **walkable floor** from the design's furniture layer (footprint − furniture
  − walls, eroded by the citizen's body height) → `godot/data/interiors.json`. Each
  interior gets its own A* grid, so citizens path *around* furniture (no walking on
  the counter). Click a building → enter; "Leave" returns to town.
- **Travel state machine** (`World.travel` / `_step_travel` / `_on_agent_arrived`):
  citizens transition in/out **only at the door** — they walk to the door, then
  cross. No more teleporting through walls or "disappearing" mid-room. Indoor
  movement never falls back to a straight line.
- **Theme-matching animated doors**: `tools/build_doors.py` template-matches each
  building's own drawn door against its matching LimeZu animated door sheet (frame 0
  == the drawn door) → `godot/data/doors.json`. The overlay aligns seamlessly and
  opens when a citizen passes through. Buildings are chosen to have real entrances:
  One-Story / Japanese / Modern homes, a Post Office (Town Center), a Mall (Print
  Shop). *(Interior exit doors are still the generic `door_1` — not yet themed.)*
- **Resolution**: window is 1920×1080 and starts maximized; camera zoomed so
  citizens read clearly; larger nameplate/bubble fonts.
- **Town meetings**: citizens convene in the Town Center daily at midday and
  whenever the founder asks something (`World.start_meeting`).
- **Brain**: `brain/server.py` WebSocket middleware → per-citizen Ollama endpoints
  (`brain/agents.yaml`). Perception→prompt→action→memory→incentives in
  `brain/mind.py`. Heuristic fallback when models are down. Needs (hunger/food),
  relationships, nightly reflection, founder grants (food/coins).
- **Eyesight**: Godot renders a vision-citizen's POV (SubViewport) → base64 PNG →
  brain → vision model. Off by default (set `vision: true` per citizen).
- **Persistence**: `brain/memory.py` (SQLite at `brain/state/<world>.sqlite`).
- **Docs**: README, ARCHITECTURE, ROADMAP, WORLD_GUIDE, MODELS, this file.

## 3. Repo map (the parts that matter)
```
godot/scripts/      World.gd (orchestrator), Agent.gd, TownBuilder.gd,
                    CharacterFrames.gd, DoorNode.gd, HUD.gd, Net.gd, GameConfig.gd
godot/data/         citizens.json (cast), world_config.json, and GENERATED:
                    town_layout.json, interiors.json (walkable floors), doors.json
brain/              server.py, mind.py, memory.py, ollama_client.py, config.py, agents.yaml
tools/              build_town.py, build_interiors.py, build_doors.py,
                    select_assets.py, copy_extras.py, copy_interiors.py, run_godot.sh
GameAssets/         LimeZu source art (Modern Interiors/Exteriors)

Asset pipeline order (re-run after changing buildings/doors/interiors):
  select_assets.py / copy_extras.py / copy_interiors.py   (curate sprites)
  build_doors.py        -> doors.json     (match animated doors to buildings)
  build_interiors.py    -> interiors.json (walkable floors)
  build_town.py         -> town_layout.json + baked ground (reads doors.json)
```

## 4. How to run & verify (this is the important part)
The environment has **no GPU/display**, but Godot 4.6 renders fine under Xvfb with
software GL, and `tools/run_godot.sh` wraps this. **You can take real screenshots —
use them to verify every visual change.** The Godot binary lives at
`tools/bin/godot` (downloaded, git-ignored; re-download from the Godot 4.6 release
if missing).

```bash
# 1. regenerate the town after editing tools/build_town.py
python3 tools/build_town.py            # also writes tools/_montages/town_preview.png

# 2. import assets into Godot (needed after new/changed assets)
bash tools/run_godot.sh import

# 3. screenshot the running town (autopilot = no brain needed)
bash tools/run_godot.sh shot tools/screenshots/out.png 6           # -> out_00.png
bash tools/run_godot.sh shot tools/screenshots/out.png 6 --overview # whole map
#    debug flags (pass after the delay): --interior "Bakery", --povtest baker

# 4. full integration (brain + town)
python3 brain/server.py --world test --port 8765 &
bash tools/run_godot.sh series tools/screenshots/g.png 1 8   # connects to the brain
```

**Fast visual iteration trick:** `tools/build_town.py` also renders
`tools/_montages/town_preview.png`, a Python composite of the exact same layout
data Godot uses — iterate on the town there (seconds) before opening the engine.
Read any PNG with the Read tool to view it.

Python deps for the brain: `pip install -r brain/requirements.txt`
(websockets, aiohttp, pyyaml; plus pillow + numpy for the tools).

## 5. Key conventions / gotchas
- **Citizen sprite slicing** (`CharacterFrames.gd`): LimeZu premade sheets are 48px
  wide, character spans TWO grid rows; pair 1 = idle, pair 2 = walk; direction
  order in a row is Right, Up, Left, Down. Verified — don't "fix" it.
- **Town is data-driven**: never hand-place sprites in Godot. Edit `build_town.py`
  (the single source of truth) and regenerate.
- **Coordinates**: props/buildings are positioned by their baseline (bottom-centre)
  for Y-sorting. Interiors live in an off-map region the camera visits.
- **Authority split**: Godot owns space/time/positions; the brain owns
  mind/memory/economy. Perception is built by Godot; persistent stats (coins,
  hunger, goals, relationships) live in the brain's SQLite.
- **GL Compatibility** renderer is required for headless software rendering
  (set in `project.godot`). Don't switch to Forward+.
- **Godot CLI args** go after `--` (see `GameConfig._parse_cli`). Useful debug
  flags: `--overview`, `--interior "<Place>"`, `--meeting`, `--povtest <id>`,
  `--agents N`, `--autopilot`.
- **Doors**: only use buildings that have a real drawn door (apartment facades
  don't, which is why they were dropped). To add a building, put its file + matching
  animated door sheet in `build_doors.PAIRS`; `b["door_anim"]` then carries
  `{sheet,fw,fh,n,ox,oy}` and `DoorNode.setup` handles any frame size.
- `.godot/` cache and `brain/state/` are git-ignored; `.import` and `.uid`
  sidecars ARE committed (standard Godot 4.6 workflow). First run re-imports.

## 6. The economy / money plan (per the founder)
Citizens need **food** and **virtual coins** to live; the founder supplies both
("give 30 food", "give 50 coins"). They earn coins by working, especially on the
founder's directives. The intended next step: a real **print-on-demand** venture
the citizens "work" on inside a building, earning real revenue that flows back as
treasury + incentives. Scaffold: `enterprises` table in `memory.py`, enterprise
detection in `server.py`. **No real financial integration is wired** — that needs
the founder's accounts/API keys.

## 7. Suggested next steps (priority order from the founder)
1. **Print-on-demand venture** (the founder's focus): design a safe brain-side tool
   that lists/sells a product (e.g. Printful/Printify API), records real revenue →
   treasury, and pays incentives. Gate behind the founder's keys. The team
   (designer/maker/seller), the Print Shop building, and the directive→work→coins
   loop are already in place.
2. **Smaller polish**: theme the interior *exit* doors (still generic `door_1`);
   the Town Center art reads "POST OFFICE" (swap if the founder wants different
   signage); more interior variety (only 2 distinct house interiors today).
3. **Deepen AI lives**: true two-party dialogues (the brain already knows who's in
   earshot), daily schedules, money/hunger pressure shaping behavior.
4. **Eyesight in practice**: test with a real vision model (`llama3.2-vision`,
   `llava`); tune POV size/zoom; let citizens act on what they see.
5. **Scale**: streaming map, a second town (`--world`), tiered minds.

See `docs/ROADMAP.md` for the fuller arc and `docs/MODELS.md` for model choices.
